import { Fragment } from "react";

export default function CreditCard({}) {
    return (
        <Fragment>
            <div className={`bg-slate-50 h-52 w-96 rounded-md`}>
                Hello world
            </div>
        </Fragment>
    )
}